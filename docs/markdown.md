# Markdown

Markdown templates, just like HTML templates, are [Handlebars](http://handlebarsjs.com/)
files.

# Helpers

`{{format_params}}`

`{{format_type}}`

`{{format_description}}`

`{{inlines}}`
