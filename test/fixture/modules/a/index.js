'use strict';

/**
 * This is a function that returns the letter a.
 * @returns {String} the letter a
 * @example
 * var a = require('a');
 * console.log(a());
 */
module.exports = function () {
  return 'a';
};
