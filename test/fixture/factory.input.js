/**
 * an area chart generator
 * @returns {area} chart
 */
var area = function() {
  /**
   * @class area
   */
  var chart = function(selection) {};

  /**
   * Sets the chart data.
   * @function
   */
  chart.data = function(_) {};

  return chart;
};
