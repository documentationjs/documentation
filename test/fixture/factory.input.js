/**
 * an area chart generator
 * @returns {area} chart
 */
var area = function() {

  /**
   * @class area
   */
  var chart = function(selection) {
  };

  /**
   * Sets the chart data.
   * @method
   */
  chart.data = function(_) {
  };

  return chart;
};
